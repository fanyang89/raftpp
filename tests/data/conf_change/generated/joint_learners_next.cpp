/**
 * This file was generated by bin2cpp v3.1.0
 * Copyright (C) 2013-2025 end2endzone.com. All rights reserved.
 * bin2cpp is open source software, see http://github.com/end2endzone/bin2cpp
 * Source code for file 'joint_learners_next.txt', last modified 1763365230.
 * Do not modify this file.
 */
#if defined(_WIN32) && !defined(_CRT_SECURE_NO_WARNINGS)
#define _CRT_SECURE_NO_WARNINGS
#endif
#include "joint_learners_next.h"
#include <string> //for std::string
#include <iostream>
#include <fstream>  //for ofstream
namespace bin2cpp
{
  class Joint_learners_nextTxtFile : public virtual bin2cpp::File
  {
  public:
    Joint_learners_nextTxtFile() { build(); }
    virtual ~Joint_learners_nextTxtFile() {}
    virtual size_t getSize() const { return 623; }
    virtual const char * getFileName() const { return "joint_learners_next.txt"; }
    virtual const char * getFilePath() const { return "joint_learners_next.txt"; }
    virtual const char * getBuffer() const { return mBuffer.c_str(); }
    void build()
    {
      mBuffer.clear();
      mBuffer.reserve(getSize()); //allocate all required memory at once to prevent reallocations
      mBuffer.append("# Verify that when a voter is demoted in a joint config, it will show up in\n# learners_next until the joint config is left, and only then will the progress\n# turn into that of a learner, without reset", 200);
      mBuffer.append("ting the progress. Note that this\n# last fact is verified by `next`, which can tell us which \"round\" the progress\n# was originally created in.\n\nsimple\nv1\n----\nvoters=(1)\n1: StateProbe match=0 next=0\n\n", 200);
      mBuffer.append("enter-joint\nv2 l1\n----\nvoters=(2)&&(1) learners_next=(1)\n1: StateProbe match=0 next=0\n2: StateProbe match=0 next=1\n\nleave-joint\n----\nvoters=(2) learners=(1)\n1: StateProbe match=0 next=0 learner\n2: Sta", 200);
      mBuffer.append("teProbe match=0 next=1\n", 23);
    }
    virtual bool save(const char * filename) const
    {
      std::ofstream f(filename, std::ios::out | std::ios::binary | std::ios::trunc);
      if (f.fail()) return false;
      size_t fileSize = getSize();
      const char * buffer = getBuffer();
      f.write(buffer, fileSize);
      f.close();
      return true;
    }
  private:
    std::string mBuffer;
  };
  const File & getJoint_learners_nextTxtFile() { static Joint_learners_nextTxtFile _instance; return _instance; }
}; //bin2cpp
